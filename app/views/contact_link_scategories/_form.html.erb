<%= form_for(@contact_link_scategory) do |f| %>
  <% if @contact_link_scategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contact_link_scategory.errors.count, "error") %> prohibited this contact_link_scategory from being saved:</h2>

      <ul>
      <% @contact_link_scategory.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :contact_id %><br />
    <%= f.number_field :contact_id %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br />
    <%= f.number_field :category_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
